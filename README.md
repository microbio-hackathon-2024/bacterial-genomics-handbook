# Don't Panic! Your Bacterial Genomics Handbook

## Building locally

The website is built with `jekyll`. Use the following command to build and serve a local version
for development:

```console
$ bundle exec jekyll serve
```
